name: pipeline-action
on: push
jobs:
    buidjob:
        runs-on: ubuntu-latest
        steps: 
        - name: build-job
          run: car /etc/passwd
    testjob:
        runs-on: ubuntu-latest
        steps:
         - name: test-job
           run: pwd
    
          

